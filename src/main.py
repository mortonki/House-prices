import sys
from src.dataloader import load_dataset
from src.preprocessing import target_encode, one_hot_encode, log_transform

def main():
    """
    Main entry point for the house prices prediction project.
    Currently, this serves as a placeholder for the full pipeline.
    """
    print("Starting House Price Prediction Pipeline...")
    
    # Example usage:
    # In a real scenario, this would involve downloading, loading, 
    # preprocessing, and training a model.
    
    # For now, we just print a success message to verify the command works.
    print("Pipeline initialized successfully.")

if __name__ == "__main__":
    main()
